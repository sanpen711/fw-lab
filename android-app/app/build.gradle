plugins {
    id("com.android.application")
}

def releaseStoreFilePath = System.getenv("FW_RELEASE_STORE_FILE")
def releaseStorePassword = System.getenv("FW_RELEASE_STORE_PASSWORD")
def releaseKeyAlias = System.getenv("FW_RELEASE_KEY_ALIAS")
def releaseKeyPassword = System.getenv("FW_RELEASE_KEY_PASSWORD")
def hasReleaseSigning = releaseStoreFilePath && releaseStorePassword && releaseKeyAlias && releaseKeyPassword

android {
    namespace = "com.fwyanjiusuo.app"
    compileSdk = 35

    defaultConfig {
        applicationId = "com.fwyanjiusuo.app"
        minSdk = 23
        targetSdk = 35
        versionCode = 6
        versionName = "1.0.5"
    }

    signingConfigs {
        release {
            if (hasReleaseSigning) {
                storeFile = file(releaseStoreFilePath)
                storePassword = releaseStorePassword
                keyAlias = releaseKeyAlias
                keyPassword = releaseKeyPassword
            }
        }
    }

    buildTypes {
        release {
            minifyEnabled = false
            shrinkResources = false
            if (hasReleaseSigning) {
                signingConfig = signingConfigs.release
            }
        }
    }

    buildFeatures {
        buildConfig = true
    }
}
